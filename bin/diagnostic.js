'use strict';

```js
//We'll learn about require later in the course
const ask = require('../lib/ask.js');

let answer = 0;

while (answer !== (>=1 && <=10) {
  answer = ask("choose a number between 1-10");
  if (answer === ) {
   console.log("Your input must be a number between 1 and 10");
   break;
  }
}
```
NOT ENOUGH TIME TO FINISH
