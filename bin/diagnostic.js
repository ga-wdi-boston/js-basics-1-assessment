'use strict';

const ask = require('../lib/ask.js');

let answer = 'not empty';

while (i = 0; i < answer.length; i += 1;) {
  if answer
  answer = ask("Guess my secret? ");
  if (answer === 'SeCrEt') {
   console.log("You guessed it!");
   break;
  }
}
```
